server {
    listen 80;
    server_name cibersystems.local www.cibersystems.local localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
    }

    # Ocultar la versión de Nginx (Buena práctica defensiva)
    server_tokens off;
}